'KaufmannDigital.GDPR.CookieConsent:Content.Cookie.Attribute':
  superTypes:
    'Neos.Neos:Content': true
  label: "${('Attribut: ' + q(node).property('label'))}"
  ui:
    label: "Attribut"
    icon: 'icon-plus'
    group: 'cookies'
  properties:
    label:
      type: string
      defaultValue: ''
      ui:
        inlineEditable: true
        inline:
          editorOptions:
            placeholder: 'Label eintragen...'
    value:
      type: string
      defaultValue: ''
      ui:
        inlineEditable: true
        inline:
          editorOptions:
            placeholder: 'Wert eintragen...'
